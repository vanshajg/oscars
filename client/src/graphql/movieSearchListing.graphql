mutation GetMovieListingBySearchTerm($options: MovieQuery!) {
  getMovies(options: $options) {
    error {
      message
    }
    movies {
      title
      movie_id
      year
      poster
    }
  }
}